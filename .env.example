# Copy to .env and fill in real values. .env is gitignored

# OpenSky Network OAuth2 client-credentials (dynamic demand engine)
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=

# Anthropic API key (routing/explanation layer)
ANTHROPIC_API_KEY=
