[pytest]
# Only collect the tests/ directory. (The deterministic engine is named test_tool.py,
# which would otherwise be picked up by pytest's default test_*.py discovery.)
testpaths = tests
# Disable the on-disk cache (avoids a harmless Windows permission warning on this repo).
addopts = -p no:cacheprovider
